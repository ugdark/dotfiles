# secret-guard.sh が使う汎用の秘密検出パターン（ERE / grep -E -i）。
# 1行1パターン。'#' 始まりと空行は無視される。
# ここには「どのプロジェクトでも秘密」な汎用パターンだけを置く。
# あなた固有の業務語（社名・案件名・顧客名など）は secret-denylist.local に書く（gitignore対象）。

# --- クラウド / API キー ---
AKIA[0-9A-Z]{16}
ASIA[0-9A-Z]{16}
AIza[0-9A-Za-z_-]{35}
ya29\.[0-9A-Za-z_-]+

# --- GitHub トークン ---
ghp_[0-9A-Za-z]{36}
gho_[0-9A-Za-z]{36}
github_pat_[0-9A-Za-z_]{22,}

# --- 決済 / SaaS ---
sk_live_[0-9a-zA-Z]{16,}
rk_live_[0-9a-zA-Z]{16,}
xox[baprs]-[0-9A-Za-z-]{10,}

# --- 秘密鍵 ---
-----BEGIN [A-Z ]*PRIVATE KEY-----

# --- ローカル絶対パス（machine固有のホームディレクトリ漏洩） ---
# 本名・メール単体は許容だが、絶対パス /Users/<username>/... は漏らさない方針。
# ※ここは正規表現形（ドットは \. でエスケープ）のみ記載する。
#   リテラル形（ドット付き）をコメント等に書くと、このファイル自身のcommitで自爆するので厳禁。
/Users/akinobu\.nishiyama

# --- 汎用の代入形（password/secret/token/api_key = 値） ---
(password|passwd|pwd|secret|token|api[_-]?key)[[:space:]]*[:=][[:space:]]*['"]?[^[:space:]'"]{6,}
